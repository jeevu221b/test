#include <stdio.h>
#include <conio.h>
int main()
{

    // Variable, Constant
    // Variable initialization
    // 4 parts INT, FLOAT, Char(5)
    // Variable type
    // Variable name
    //  Assignment operator, =
    // Variable value
    int a, b, multi;

    // datatype variable_name = "value";

    // int = %d
    // char = %s
    // float = %f

    // printf("format specifier", variable name);
    printf("Enter 1st no. ");
    scanf("%d", &a);
    printf("Enter 2nd no. ");
    scanf("%d", &b);
    multi = a * b;
    printf("The mulplication = %d", multi);

    // scanf("format specifer", &varible name);
    // printf("%f", deci);
}
